const ip = process.env.REACT_APP_IP;
const port = process.env.REACT_APP_PORT;

// export const baseIp = `http://${ip}:${port}`;
export const baseIp = `http://localhost:${8222}`;
console.log("baseIp", baseIp);
